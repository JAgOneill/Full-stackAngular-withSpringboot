import { RegisternewAdminhere } from './registernew-adminhere';

describe('RegisternewAdminhere', () => {
  it('should create an instance', () => {
    expect(new RegisternewAdminhere()).toBeTruthy();
  });
});
